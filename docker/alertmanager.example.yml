route:
  receiver: operations
  group_by: [alertname]
  group_wait: 30s
  repeat_interval: 4h
receivers:
  - name: operations
    webhook_configs:
      - url: https://YOUR-OPERATIONS-WEBHOOK.example.org/alerts
        send_resolved: true
